show_debug_message("game loaded");
// decode .sav file

// load .sav results into player_stats list

// max out hp
oPlayer.hp = oPlayer.max_hp;
// save file room to var
var load_room;
room_goto(asset_get_index(load_room));