alpha=1;
with obBlack alpha=1
with obBackSpells alpha=1
with obOverview alpha=1
with obDrawSpellAssign alpha=1
with obMission alpha=1
with obStart { depth=-7 x=xstart y=ystart }
with obHeroLab alpha=1
with obTrophyNext alpha=1
with obChoose value=-1
with obStats depth=-7
with obArcade { depth=-7 x=xstart y=ystart arcadeMode = 0 gameCount=-1 }


Begin=0

//for (i=0; i<=missionMax; i+=1) { audio_stop_sound(audioLine[i]) }

