/*for (i=0; i<=missionMax; i+=1)
{
    audio_stop_sound(audioLine[i])
    if i=missionType {
        audio_play_sound(audioLine[missionType], 2, 0)
        audio_sound_gain(audioMusic[0], 0.05, 30)
    }
}
